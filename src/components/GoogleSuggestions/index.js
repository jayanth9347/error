const GoogleSuggestions = props => {
  const {suggestionsList} = props
  const {suggestion, id} = suggestion[0]
  return (
    <div>
      {' '}
      <h1> Hello </h1>{' '}
    </div>
  )
}

export default GoogleSuggestions
